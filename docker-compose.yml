version: "3.9"

services:
  regresion:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/app/notebooks
      - ./data:/app/data
      - ./src:/app/src
      - ./reports:/app/reports
    stdin_open: true
    tty: true
